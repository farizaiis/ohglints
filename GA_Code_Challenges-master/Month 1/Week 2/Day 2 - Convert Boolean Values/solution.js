function boolToWord( bool ){
    //...
    if (bool === true) {
      return bool = "Yes"
    } else {
      return bool = "No"
    }
  }

  function boolToWord( bool ){
    return bool ? "Yes" : "No" ;
  }